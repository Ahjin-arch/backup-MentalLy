<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado seleccionado -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <solid android:color="?android:attr/windowBackground" /> <!-- Amarillo para estado seleccionado -->
            <corners android:radius="16dp" />
            <stroke android:width="2dp" android:color="@android:color/darker_gray" /><!-- Bordes redondeados -->
        </shape>
    </item>

    <!-- Estado no seleccionado -->
    <item android:state_checked="false">
        <shape android:shape="rectangle">
            <solid android:color="?android:attr/windowBackground" /> <!-- Blanco para estado no seleccionado -->
            <corners android:radius="10dp" /> <!-- Bordes redondeados -->
        </shape>
    </item>
</selector>


